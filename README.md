PL/SQL GOTO & Collections 
Description

This  demonstrates basic PL/SQL programming concepts including:

GOTO Statement – Shows how to use labels and GOTO for loop control.

Collections + GOTO – Uses a nested table collection along with GOTO to iterate through elements.
